signal loc_sin, loc_cos

left := (loc_sin[t] < 0.1 and loc_sin[t] > -0.1) and (loc_cos[t] > 0.9)
obstacle := (loc_sin[t] < 0.6 and loc_sin[t] > 0.5) and (loc_cos[t] > 0.7)

ev_left := ev_[0, 20] left
never_obstacle := alw_[0, 20] (not obstacle)

task_avoid_obstacle := ev_left and never_obstacle