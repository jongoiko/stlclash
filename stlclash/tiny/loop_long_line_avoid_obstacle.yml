specs: long_line_loop.stl
action_names:
  action: "action"
obs_names:
  loc_sin: "np.sin(2 * np.pi * obs / 20)"
  loc_cos: "np.cos(2 * np.pi * obs / 20)"
keep_old_reward: False
multi_objective: True
reward_formulas:
  ev_left:
    past_horizon: 0
  never_obstacle:
    past_horizon: 0
