specs: long_line_loop.stl
action_names:
  action: "action"
obs_names:
  loc_sin: "np.sin(2 * np.pi * obs / 10)"
  loc_cos: "np.cos(2 * np.pi * obs / 10)"
keep_old_reward: False
multi_objective: True
reward_formulas:
  ev_left:
    past_horizon: 0
  never_obstacle:
    past_horizon: 0
obs_formulas:
  ev_left:
    obs_name: 'ev_left'
    past_horizon: 20
  never_obstacle:
    obs_name: 'never_obstacle'
    past_horizon: 20
